const template = require('./componentTemplate');
const indexTemplate = require('./indexTemplate');

module.exports = {
    template,
    indexTemplate,
    prettier: true,
    prettierConfig: {
        singleQuote: true,
    },
};
